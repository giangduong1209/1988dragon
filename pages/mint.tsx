import { Default } from 'components/layouts/Default';
import { Mint } from 'components/templates/Mint';

const MintPage = () => {

  return (
    <Default pageName="Minting">
      <Mint />
    </Default>
  );
};

export default MintPage;
